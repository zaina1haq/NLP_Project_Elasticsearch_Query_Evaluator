[
  {
    "task": "Find all documents associated with country code \"jp\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "jp"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countryKeys": "jp"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single term query. | [Field Validity] 'countryKeys' is the valid keyword field for country codes. | [Operator Correctness] 'term' is the correct operator for exact match on a keyword field. | [Task Alignment] Returns exactly documents associated with country code 'jp'."
  },
  {
    "task": "Which articles were published between 1987-01-01 and 1987-03-31?",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-03-31"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-03-31"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct range query at the query level. | [Field Validity] 'date' is the valid date field in the mapping. | [Operator Correctness] Operators ['gte', 'lte'] are exactly correct for 'published between 1987-01-01 and 1987-03-31'. | [Task Alignment] Returns exactly the documents in the specified date window."
  },
  {
    "task": "Retrieve articles mentioning \"oil prices\" in their title or body text.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "oil prices",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "oil prices",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct multi_match query with 'query' and 'fields' both present. | [Field Validity] Both 'title' and 'content' are valid text fields. | [Operator Correctness] 'multi_match' is the correct operator for searching across multiple fields simultaneously. | [Task Alignment] Returns all documents where 'oil prices' appears in either title or content."
  },
  {
    "task": "Retrieve articles that have a georeference entry where country_code is \"fr\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "fr"
            }
          }
        }
      }
    },
    "submission": {
      "bool": {
        "must": [
          {
            "match": {
              "title": "value"
            }
          }
        ]
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Completely invalid Elasticsearch DSL \u2014 no 'nested' wrapper, no valid 'query' key at the right level. | [Field Validity] Nested path and field are not correctly referenced. | [Operator Correctness] No valid operator is applied inside the nested context. | [Task Alignment] Would fail to parse or return entirely unrelated documents."
  },
  {
    "task": "Get every document published in 1987.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-12-31"
          }
        }
      }
    },
    "submission": {
      "filter": {
        "term": {
          "topics": "trade"
        }
      },
      "sort": "date"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely malformed DSL. | [Field Validity] No valid date field is correctly referenced. | [Operator Correctness] No range operator is applied. | [Task Alignment] Would fail to parse or return entirely irrelevant documents."
  },
  {
    "task": "Retrieve every article linked to the country key \"gb\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "gb"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countryKeys": "gb"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single term query. | [Field Validity] 'countryKeys' is the valid keyword field for country codes. | [Operator Correctness] 'term' is the correct operator for exact match on a keyword field. | [Task Alignment] Returns exactly documents associated with country code 'gb'."
  },
  {
    "task": "Filter the index to documents that are published after 1987-09-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-09-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-09-02"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct range query structure. | [Field Validity] 'date' field is correct. | [Operator Correctness] Operator direction is correct but boundary date is slightly off ('1987-09-02' vs '1987-09-01'). Minor precision issue. | [Task Alignment] Returns mostly correct documents but may miss or include a small number of edge documents due to the boundary difference."
  },
  {
    "task": "Retrieve every article linked to the country key \"us\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "us"
        }
      }
    },
    "submission": {
      "query": {
        "match_all": {}
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Syntactically valid but semantically empty \u2014 match_all returns every document. | [Field Validity] No field is targeted. | [Operator Correctness] match_all is not a filtering operator. | [Task Alignment] Returns all documents in the index with no filtering by country 'us' \u2014 completely fails the task."
  },
  {
    "task": "Which documents have a nested georeference whose country_code equals \"gb\"?",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "gb"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "gb"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct nested query with 'path' and inner 'query' both present. | [Field Validity] 'georeferences' is a valid nested field; 'georeferences.country_code' is the correct dotted path. | [Operator Correctness] 'term' is correct for exact matching on the keyword subfield 'country_code'. | [Task Alignment] Returns exactly the documents with a nested georeference entry for country_code 'gb'."
  },
  {
    "task": "Get documents where countryKeys includes \"ca\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "ca"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "places": "ca"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'places' stores human-readable place name strings, not ISO country codes \u2014 the correct field is 'countryKeys'. Wrong field but exists in the mapping. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Will not find documents by country code 'ca' since 'places' contains names not codes. Results will be empty or wrong."
  },
  {
    "task": "Multi-field search for \"earn\" covering title and content.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "earn",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "earn",
          "fields": [
            "headline",
            "body"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct multi_match structure. | [Field Validity] 'headline' and 'body' do not exist in the mapping \u2014 the correct fields are 'title' and 'content'. Both field names are wrong. | [Operator Correctness] 'multi_match' is the right operator. | [Task Alignment] Returns zero results because neither field exists in the index."
  },
  {
    "task": "Retrieve every document where the content field contains the word \"crude\".",
    "reference": {
      "query": {
        "match": {
          "content": "crude"
        }
      }
    },
    "submission": {
      "search": {
        "match": {
          "content": "grain"
        }
      },
      "order": "desc"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Invalid top-level keys \u2014 not a valid Elasticsearch query DSL structure. | [Field Validity] Fields referenced either don't exist or are in wrong positions. | [Operator Correctness] No valid operator is correctly applied. | [Task Alignment] This query would either fail to parse or return completely unrelated results."
  },
  {
    "task": "Retrieve all documents that are tagged with the topic \"grain\".",
    "reference": {
      "query": {
        "term": {
          "topics": "grain"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "topics": "grain"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Query structure is valid. | [Field Validity] 'topics' field is correct. | [Operator Correctness] 'match' is imprecise on a keyword field \u2014 it performs analysis that may split or normalize the value, causing unexpected matches or misses. 'term' is required for exact keyword matching. | [Task Alignment] May return broadly correct results but with precision issues for multi-word topic values like 'grain'."
  },
  {
    "task": "Find all news articles whose body text mentions \"acq\".",
    "reference": {
      "query": {
        "match": {
          "content": "acq"
        }
      }
    },
    "submission": {
      "filter": {
        "term": {
          "topics": "trade"
        }
      },
      "sort": "date"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Invalid top-level keys \u2014 not a valid Elasticsearch query DSL structure. | [Field Validity] Fields referenced either don't exist or are in wrong positions. | [Operator Correctness] No valid operator is correctly applied. | [Task Alignment] This query would either fail to parse or return completely unrelated results."
  },
  {
    "task": "Get all documents whose topics field contains \"earn\".",
    "reference": {
      "query": {
        "term": {
          "topics": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": "earn"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct term query at the query level. | [Field Validity] 'topics' is a valid keyword field in the mapping. | [Operator Correctness] 'term' is the correct operator for exact-match on keyword fields. | [Task Alignment] Returns exactly documents tagged with topic 'earn'."
  },
  {
    "task": "Pull all documents whose content includes the keyword \"money-fx\".",
    "reference": {
      "query": {
        "match": {
          "content": "money-fx"
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "money-fx"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Unnecessarily wrapped in a bool/must but structurally valid; however no filter clause when none is needed is not wrong \u2014 this sub-case represents having the right match but inside the wrong clause type without any filter. | [Field Validity] 'content' field is correct. | [Operator Correctness] 'match' is correct. | [Task Alignment] This particular variant is effectively equivalent; for diversity this entry models the case where the bool wrapper adds no filter and the evaluator notes unnecessary complexity but correct results."
  },
  {
    "task": "Get articles both discussing and categorised under \"acq\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "topics": "acq"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] The bool query is valid but only has a 'must' clause \u2014 the 'filter' clause is entirely absent. | [Field Validity] The must clause field is correct. | [Operator Correctness] The must operator is correct; the filter operator doesn't exist to evaluate. | [Task Alignment] The query only satisfies the content-matching part of the task. The date/topic/country constraint is completely missing, returning too many documents."
  },
  {
    "task": "Pull all documents whose content includes the keyword \"grain\".",
    "reference": {
      "query": {
        "match": {
          "content": "grain"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "content": "grain"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Query is structurally parseable. | [Field Validity] 'content' is a valid field. | [Operator Correctness] 'term' on a text field is a major operator error \u2014 'content' is analyzed/tokenized so term queries almost never match unless the value is a single token exactly as stored. | [Task Alignment] Will return near-zero results for 'grain' because analyzed text fields require 'match', not 'term'."
  },
  {
    "task": "Retrieve every document where the content field contains the word \"earn\".",
    "reference": {
      "query": {
        "match": {
          "content": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "content": "earn"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Query is structurally parseable. | [Field Validity] 'content' is a valid field. | [Operator Correctness] 'term' on a text field is a major operator error \u2014 'content' is analyzed/tokenized so term queries almost never match unless the value is a single token exactly as stored. | [Task Alignment] Will return near-zero results for 'earn' because analyzed text fields require 'match', not 'term'."
  },
  {
    "task": "Get all documents discussing \"earn\" from 1987 onwards.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "earn"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "gte": "1987-01-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "earn"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "published_at": {
                  "gte": "1987-01-01"
                }
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct bool structure with must and filter clauses. | [Field Validity] The must clause field is correct; the filter clause references a non-existent field \u2014 a single field name fix is all that's needed. | [Operator Correctness] Operators (match/term/range) are all correct. | [Task Alignment] The query logic is right but the invalid filter field means the constraint is not applied, making the result set over-broad."
  },
  {
    "task": "Find documents containing a georeference with country_code \"gb\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "gb"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "match": {
              "georeferences.country_code": "gb"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct nested wrapper with 'path' and inner 'query'. | [Field Validity] Path and field name are both correct. | [Operator Correctness] 'match' is used on a keyword subfield where 'term' is required for exact matching. This is a minor operator precision issue. | [Task Alignment] Will mostly return correct results but 'match' may produce unexpected behavior on the keyword field 'georeferences.country_code'."
  },
  {
    "task": "Get every document published between 1987-01-01 and 1987-03-31.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-03-31"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-03-31"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct range query structure. | [Field Validity] 'date' is the correct field. | [Operator Correctness] The comparison operator is flipped (gte\u2194lte), inverting the date window. This is a significant logic error that returns the opposite set of documents. | [Task Alignment] The field and structure are right but the result set is the complement of what was asked for."
  },
  {
    "task": "Get all documents whose topics field contains \"crude\".",
    "reference": {
      "query": {
        "term": {
          "topics": "crude"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "topics": "crude"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Query structure is valid. | [Field Validity] 'topics' field is correct. | [Operator Correctness] 'match' is imprecise on a keyword field \u2014 it performs analysis that may split or normalize the value, causing unexpected matches or misses. 'term' is required for exact keyword matching. | [Task Alignment] May return broadly correct results but with precision issues for multi-word topic values like 'crude'."
  },
  {
    "task": "Which Reuters articles have \"trade\" mentioned in the content?",
    "reference": {
      "query": {
        "match": {
          "content": "trade"
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "trade"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Unnecessarily wrapped in a bool/must but structurally valid; however no filter clause when none is needed is not wrong \u2014 this sub-case represents having the right match but inside the wrong clause type without any filter. | [Field Validity] 'content' field is correct. | [Operator Correctness] 'match' is correct. | [Task Alignment] This particular variant is effectively equivalent; for diversity this entry models the case where the bool wrapper adds no filter and the evaluator notes unnecessary complexity but correct results."
  },
  {
    "task": "Get \"earn\" articles with the most recent ones at the top.",
    "reference": {
      "query": {
        "match": {
          "content": "earn"
        }
      },
      "sort": [
        {
          "date": {
            "order": "desc"
          }
        }
      ]
    },
    "submission": {
      "query": {
        "match": {
          "content": "earn"
        }
      },
      "sort": [
        {
          "date": {
            "order": "desc"
          }
        }
      ]
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct query clause plus a valid top-level 'sort' array. | [Field Validity] Both the query field and the sort field ('date') exist in the mapping. | [Operator Correctness] Sort order 'desc' is correct for the task. | [Task Alignment] Returns the right documents in the correct order."
  },
  {
    "task": "Search for articles that discuss \"earn\" anywhere in their content.",
    "reference": {
      "query": {
        "match": {
          "content": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "earn"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Unnecessarily wrapped in a bool/must but structurally valid; however no filter clause when none is needed is not wrong \u2014 this sub-case represents having the right match but inside the wrong clause type without any filter. | [Field Validity] 'content' field is correct. | [Operator Correctness] 'match' is correct. | [Task Alignment] This particular variant is effectively equivalent; for diversity this entry models the case where the bool wrapper adds no filter and the evaluator notes unnecessary complexity but correct results."
  },
  {
    "task": "Multi-field search for \"export\" covering title and content.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "export",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "export",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct multi_match query with 'query' and 'fields' both present. | [Field Validity] Both 'title' and 'content' are valid text fields. | [Operator Correctness] 'multi_match' is the correct operator for searching across multiple fields simultaneously. | [Task Alignment] Returns all documents where 'export' appears in either title or content."
  },
  {
    "task": "Retrieve \"acq\" articles from oldest to newest.",
    "reference": {
      "query": {
        "term": {
          "topics": "acq"
        }
      },
      "sort": [
        {
          "date": {
            "order": "asc"
          }
        }
      ]
    },
    "submission": {
      "filter": {
        "match": {
          "content": "x"
        }
      },
      "order": "asc"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] 'filter' and 'order' are not valid Elasticsearch top-level keys \u2014 the correct keys are 'query' and 'sort'. This query would fail to parse. | [Field Validity] No valid field is correctly targeted in the right position. | [Operator Correctness] No valid operator is applied. | [Task Alignment] Completely invalid \u2014 would throw a parse error and return no results."
  },
  {
    "task": "Find documents where either the title or the content contains \"earn\".",
    "reference": {
      "query": {
        "multi_match": {
          "query": "earn",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "earn",
          "fields": [
            "title"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct multi_match structure. | [Field Validity] 'title' is a valid field. | [Operator Correctness] 'multi_match' is correct but underused \u2014 only one field is listed. | [Task Alignment] Searches only 'title', missing all documents where 'earn' appears only in 'content'. Half the required fields are covered."
  },
  {
    "task": "Get documents where countryKeys includes \"gb\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "gb"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "countryKeys": "gb"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Valid query structure. | [Field Validity] 'countryKeys' is the correct field. | [Operator Correctness] 'match' on a keyword field is imprecise \u2014 country codes should be matched exactly with 'term', not analyzed with 'match'. | [Task Alignment] May mostly work for single-token country codes like 'gb' but is semantically wrong and fragile for multi-part codes."
  },
  {
    "task": "Retrieve documents whose title contains \"money-fx\" and that predate June 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "lte": "1987-06-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "lte": "1987-06-01"
                }
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct bool query with both 'must' and 'filter' clauses properly composed. | [Field Validity] All field names match the index mapping exactly. | [Operator Correctness] match/term/range operators are all appropriate for their respective field types. | [Task Alignment] Returns exactly the documents satisfying both the relevance and the constraint conditions."
  },
  {
    "task": "Get articles both discussing and categorised under \"acq\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "topics": "acq"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "tags": "acq"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct bool structure with must and filter clauses. | [Field Validity] The must clause field is correct; the filter clause references a non-existent field \u2014 a single field name fix is all that's needed. | [Operator Correctness] Operators (match/term/range) are all correct. | [Task Alignment] The query logic is right but the invalid filter field means the constraint is not applied, making the result set over-broad."
  },
  {
    "task": "Show me articles that belong to the \"grain\" topic.",
    "reference": {
      "query": {
        "term": {
          "topics": "grain"
        }
      }
    },
    "submission": {
      "search": {
        "match": {
          "content": "grain"
        }
      },
      "order": "desc"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Top-level structure is invalid \u2014 not a valid Elasticsearch query. | [Field Validity] No valid mapping field is correctly targeted. | [Operator Correctness] No valid Elasticsearch operator is used correctly. | [Task Alignment] Would throw a parse error or return completely irrelevant documents."
  },
  {
    "task": "Which Reuters articles have \"grain\" mentioned in the content?",
    "reference": {
      "query": {
        "match": {
          "content": "grain"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "body": "grain"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct match clause structure. | [Field Validity] The field 'body' does not exist in the mapping \u2014 correct field is 'content'. This causes zero results. | [Operator Correctness] 'match' operator is appropriate for text search. | [Task Alignment] Intent is correct but the non-existent field means no documents are retrieved."
  },
  {
    "task": "Filter by countryKeys to return only \"br\" documents.",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "br"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "countryKeys": "br"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Valid query structure. | [Field Validity] 'countryKeys' is the correct field. | [Operator Correctness] 'match' on a keyword field is imprecise \u2014 country codes should be matched exactly with 'term', not analyzed with 'match'. | [Task Alignment] May mostly work for single-token country codes like 'br' but is semantically wrong and fragile for multi-part codes."
  },
  {
    "task": "Find every article labelled under the topic category \"crude\".",
    "reference": {
      "query": {
        "term": {
          "topics": "crude"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "tags": "crude"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Structurally valid term query. | [Field Validity] 'tags' does not exist in the index mapping \u2014 the correct field is 'topics'. This is a critical field name error. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Returns zero results because the field doesn't exist. The intent is clear but the field name error is fatal."
  },
  {
    "task": "Search for \"export\" across both the title and content fields.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "export",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "filter": {
            "match": {
              "content": "oil"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Invalid Elasticsearch DSL \u2014 wrong top-level keys or missing 'query' wrapper. | [Field Validity] No valid mapping fields are correctly referenced. | [Operator Correctness] No valid operator is applied. | [Task Alignment] Would fail to parse or return completely unrelated documents."
  },
  {
    "task": "Get every document published before 1987-06-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-06-01"
          }
        }
      }
    },
    "submission": {
      "filter": {
        "term": {
          "topics": "trade"
        }
      },
      "sort": "date"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely malformed DSL. | [Field Validity] No valid date field is correctly referenced. | [Operator Correctness] No range operator is applied. | [Task Alignment] Would fail to parse or return entirely irrelevant documents."
  },
  {
    "task": "Filter by countryKeys to return only \"jp\" documents.",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "jp"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "countryKeys": "jp"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Valid query structure. | [Field Validity] 'countryKeys' is the correct field. | [Operator Correctness] 'match' on a keyword field is imprecise \u2014 country codes should be matched exactly with 'term', not analyzed with 'match'. | [Task Alignment] May mostly work for single-token country codes like 'jp' but is semantically wrong and fragile for multi-part codes."
  },
  {
    "task": "Get all documents whose topics field contains \"acq\".",
    "reference": {
      "query": {
        "term": {
          "topics": "acq"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": ""
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'topics' field is valid. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The value is empty string instead of 'acq' \u2014 returns zero relevant documents. Intent is partially right (correct field and operator) but the value is missing."
  },
  {
    "task": "Which documents have a nested georeference whose country_code equals \"gb\"?",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "gb"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "terms": {
          "content": [
            "oil",
            "grain"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Completely invalid Elasticsearch DSL \u2014 no 'nested' wrapper, no valid 'query' key at the right level. | [Field Validity] Nested path and field are not correctly referenced. | [Operator Correctness] No valid operator is applied inside the nested context. | [Task Alignment] Would fail to parse or return entirely unrelated documents."
  },
  {
    "task": "Find articles from country \"gb\" published in 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match_all": {}
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "gb"
              }
            },
            {
              "range": {
                "date": {
                  "gte": "1987-01-01",
                  "lte": "1987-12-31"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match_all": {}
            }
          ],
          "filter": [
            {
              "term": {
                "countries": "gb"
              }
            },
            {
              "range": {
                "date": {
                  "gte": "1987-01-01",
                  "lte": "1987-12-31"
                }
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct bool structure with must and filter clauses. | [Field Validity] The must clause field is correct; the filter clause references a non-existent field \u2014 a single field name fix is all that's needed. | [Operator Correctness] Operators (match/term/range) are all correct. | [Task Alignment] The query logic is right but the invalid filter field means the constraint is not applied, making the result set over-broad."
  },
  {
    "task": "Filter by countryKeys to return only \"gb\" documents.",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "gb"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countries": "gb"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct term query structure. | [Field Validity] 'countries' does not exist in the mapping at all \u2014 the correct field is 'countryKeys'. Fatal field name error. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Get \"earn\" articles with the most recent ones at the top.",
    "reference": {
      "query": {
        "match": {
          "content": "earn"
        }
      },
      "sort": [
        {
          "date": {
            "order": "desc"
          }
        }
      ]
    },
    "submission": {
      "filter": {
        "match": {
          "content": "x"
        }
      },
      "order": "desc"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] 'filter' and 'order' are not valid Elasticsearch top-level keys \u2014 the correct keys are 'query' and 'sort'. This query would fail to parse. | [Field Validity] No valid field is correctly targeted in the right position. | [Operator Correctness] No valid operator is applied. | [Task Alignment] Completely invalid \u2014 would throw a parse error and return no results."
  },
  {
    "task": "Retrieve all documents that are tagged with the topic \"money-fx\".",
    "reference": {
      "query": {
        "term": {
          "topics": "money-fx"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "topics": "money-fx"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Query structure is valid. | [Field Validity] 'topics' field is correct. | [Operator Correctness] 'match' is imprecise on a keyword field \u2014 it performs analysis that may split or normalize the value, causing unexpected matches or misses. 'term' is required for exact keyword matching. | [Task Alignment] May return broadly correct results but with precision issues for multi-word topic values like 'money-fx'."
  },
  {
    "task": "Run a search for \"money-fx\" in title and content simultaneously.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "money-fx",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "money-fx",
          "fields": [
            "headline",
            "body"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct multi_match structure. | [Field Validity] 'headline' and 'body' do not exist in the mapping \u2014 the correct fields are 'title' and 'content'. Both field names are wrong. | [Operator Correctness] 'multi_match' is the right operator. | [Task Alignment] Returns zero results because neither field exists in the index."
  },
  {
    "task": "Show me all records tagged with the country code \"jp\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "jp"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countries": "jp"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct term query structure. | [Field Validity] 'countries' does not exist in the mapping at all \u2014 the correct field is 'countryKeys'. Fatal field name error. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Find all documents associated with country code \"ca\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "ca"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countryKeys": "ca"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single term query. | [Field Validity] 'countryKeys' is the valid keyword field for country codes. | [Operator Correctness] 'term' is the correct operator for exact match on a keyword field. | [Task Alignment] Returns exactly documents associated with country code 'ca'."
  },
  {
    "task": "Find documents containing a georeference with country_code \"jp\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "jp"
            }
          }
        }
      }
    },
    "submission": {
      "bool": {
        "must": [
          {
            "match": {
              "title": "value"
            }
          }
        ]
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Completely invalid Elasticsearch DSL \u2014 no 'nested' wrapper, no valid 'query' key at the right level. | [Field Validity] Nested path and field are not correctly referenced. | [Operator Correctness] No valid operator is applied inside the nested context. | [Task Alignment] Would fail to parse or return entirely unrelated documents."
  },
  {
    "task": "Retrieve articles associated with country code \"us\" that discuss \"grain\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "grain"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "us"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "grain"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "us"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct bool query with both 'must' and 'filter' clauses properly composed. | [Field Validity] All field names match the index mapping exactly. | [Operator Correctness] match/term/range operators are all appropriate for their respective field types. | [Task Alignment] Returns exactly the documents satisfying both the relevance and the constraint conditions."
  },
  {
    "task": "Retrieve all documents that are tagged with the topic \"acq\".",
    "reference": {
      "query": {
        "term": {
          "topics": "acq"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": "acq"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct term query at the query level. | [Field Validity] 'topics' is a valid keyword field in the mapping. | [Operator Correctness] 'term' is the correct operator for exact-match on keyword fields. | [Task Alignment] Returns exactly documents tagged with topic 'acq'."
  },
  {
    "task": "Find documents containing a georeference with country_code \"jp\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "jp"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "georeferences.country_code": "jp"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Missing the required 'nested' wrapper \u2014 a plain term query cannot traverse nested objects in Elasticsearch. | [Field Validity] The dotted field path 'georeferences.country_code' is correct. | [Operator Correctness] 'term' is the right operator but it's applied at the wrong query level. | [Task Alignment] Elasticsearch ignores plain term queries on nested fields \u2014 returns empty or wrong results."
  },
  {
    "task": "Search inside the georeferences array for entries with country_code \"gb\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "gb"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georefs",
          "query": {
            "term": {
              "georeferences.country_code": "gb"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Nested wrapper structure is correct (path + query present). | [Field Validity] The nested path 'georefs' does not match the mapping \u2014 the correct path is 'georeferences'. The inner field name is correct. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The query cannot traverse the nested objects because the path is wrong \u2014 returns zero results despite correct inner query logic."
  },
  {
    "task": "Retrieve every article linked to the country key \"gb\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "gb"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "places": "gb"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'places' stores human-readable place name strings, not ISO country codes \u2014 the correct field is 'countryKeys'. Wrong field but exists in the mapping. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Will not find documents by country code 'gb' since 'places' contains names not codes. Results will be empty or wrong."
  },
  {
    "task": "Get all documents whose topics field contains \"acq\".",
    "reference": {
      "query": {
        "term": {
          "topics": "acq"
        }
      }
    },
    "submission": {
      "bool": {
        "must": [
          {
            "match": {
              "title": "value"
            }
          }
        ]
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Top-level structure is invalid \u2014 not a valid Elasticsearch query. | [Field Validity] No valid mapping field is correctly targeted. | [Operator Correctness] No valid Elasticsearch operator is used correctly. | [Task Alignment] Would throw a parse error or return completely irrelevant documents."
  },
  {
    "task": "Search for articles that discuss \"trade\" anywhere in their content.",
    "reference": {
      "query": {
        "match": {
          "content": "trade"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "content": "trade"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Query is structurally parseable. | [Field Validity] 'content' is a valid field. | [Operator Correctness] 'term' on a text field is a major operator error \u2014 'content' is analyzed/tokenized so term queries almost never match unless the value is a single token exactly as stored. | [Task Alignment] Will return near-zero results for 'trade' because analyzed text fields require 'match', not 'term'."
  },
  {
    "task": "Multi-field search for \"earn\" covering title and content.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "earn",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "earn",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct multi_match query with 'query' and 'fields' both present. | [Field Validity] Both 'title' and 'content' are valid text fields. | [Operator Correctness] 'multi_match' is the correct operator for searching across multiple fields simultaneously. | [Task Alignment] Returns all documents where 'earn' appears in either title or content."
  },
  {
    "task": "Retrieve every document where the content field contains the word \"crude\".",
    "reference": {
      "query": {
        "match": {
          "content": "crude"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "content": "crude"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Query is structurally parseable. | [Field Validity] 'content' is a valid field. | [Operator Correctness] 'term' on a text field is a major operator error \u2014 'content' is analyzed/tokenized so term queries almost never match unless the value is a single token exactly as stored. | [Task Alignment] Will return near-zero results for 'crude' because analyzed text fields require 'match', not 'term'."
  },
  {
    "task": "Get articles both discussing and categorised under \"acq\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "topics": "acq"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "topics": "acq"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct bool query with both 'must' and 'filter' clauses properly composed. | [Field Validity] All field names match the index mapping exactly. | [Operator Correctness] match/term/range operators are all appropriate for their respective field types. | [Task Alignment] Returns exactly the documents satisfying both the relevance and the constraint conditions."
  },
  {
    "task": "Find documents where either the title or the content contains \"export\".",
    "reference": {
      "query": {
        "multi_match": {
          "query": "export",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "export",
          "fields": [
            "title"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct multi_match structure. | [Field Validity] 'title' is a valid field. | [Operator Correctness] 'multi_match' is correct but underused \u2014 only one field is listed. | [Task Alignment] Searches only 'title', missing all documents where 'export' appears only in 'content'. Half the required fields are covered."
  },
  {
    "task": "Retrieve every article linked to the country key \"br\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "br"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countries": "br"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct term query structure. | [Field Validity] 'countries' does not exist in the mapping at all \u2014 the correct field is 'countryKeys'. Fatal field name error. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Retrieve documents whose title contains \"money-fx\" and that predate June 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "lte": "1987-06-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] The bool query is valid but only has a 'must' clause \u2014 the 'filter' clause is entirely absent. | [Field Validity] The must clause field is correct. | [Operator Correctness] The must operator is correct; the filter operator doesn't exist to evaluate. | [Task Alignment] The query only satisfies the content-matching part of the task. The date/topic/country constraint is completely missing, returning too many documents."
  },
  {
    "task": "Find every article labelled under the topic category \"crude\".",
    "reference": {
      "query": {
        "term": {
          "topics": "crude"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": "crude"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct term query at the query level. | [Field Validity] 'topics' is a valid keyword field in the mapping. | [Operator Correctness] 'term' is the correct operator for exact-match on keyword fields. | [Task Alignment] Returns exactly documents tagged with topic 'crude'."
  },
  {
    "task": "Get all documents whose georeferences include an entry for country_code \"ca\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "ca"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "georeferences.country_code": "ca"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Missing the required 'nested' wrapper \u2014 a plain term query cannot traverse nested objects in Elasticsearch. | [Field Validity] The dotted field path 'georeferences.country_code' is correct. | [Operator Correctness] 'term' is the right operator but it's applied at the wrong query level. | [Task Alignment] Elasticsearch ignores plain term queries on nested fields \u2014 returns empty or wrong results."
  },
  {
    "task": "Filter the index to documents tagged \"acq\" in their topics list.",
    "reference": {
      "query": {
        "term": {
          "topics": "acq"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "tags": "acq"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Structurally valid term query. | [Field Validity] 'tags' does not exist in the index mapping \u2014 the correct field is 'topics'. This is a critical field name error. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Returns zero results because the field doesn't exist. The intent is clear but the field name error is fatal."
  },
  {
    "task": "Get every document published after 1987-09-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-09-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-09-01"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct range query structure. | [Field Validity] 'date' is the correct field. | [Operator Correctness] The comparison operator is flipped (gte\u2194lte), inverting the date window. This is a significant logic error that returns the opposite set of documents. | [Task Alignment] The field and structure are right but the result set is the complement of what was asked for."
  },
  {
    "task": "Which articles were published after 1987-09-01?",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-09-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "terms": {
          "content": [
            "oil",
            "grain"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely malformed DSL. | [Field Validity] No valid date field is correctly referenced. | [Operator Correctness] No range operator is applied. | [Task Alignment] Would fail to parse or return entirely irrelevant documents."
  },
  {
    "task": "Get \"earn\" articles with the most recent ones at the top.",
    "reference": {
      "query": {
        "match": {
          "content": "earn"
        }
      },
      "sort": [
        {
          "date": {
            "order": "desc"
          }
        }
      ]
    },
    "submission": {
      "query": {
        "match": {
          "content": "earn"
        }
      },
      "sort": [
        {
          "nonexistent_field": {
            "order": "desc"
          }
        }
      ]
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Query and sort clauses are both present and structurally correct. | [Field Validity] The query field is correct; the sort field 'nonexistent_field' does not exist in the mapping. | [Operator Correctness] Sort order 'desc' is correct. | [Task Alignment] The query part is perfect but sorting on a non-existent field will either error or fall back to default ordering, failing to satisfy the sort requirement."
  },
  {
    "task": "Retrieve articles that have a georeference entry where country_code is \"us\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "us"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "terms": {
          "content": [
            "oil",
            "grain"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Completely invalid Elasticsearch DSL \u2014 no 'nested' wrapper, no valid 'query' key at the right level. | [Field Validity] Nested path and field are not correctly referenced. | [Operator Correctness] No valid operator is applied inside the nested context. | [Task Alignment] Would fail to parse or return entirely unrelated documents."
  },
  {
    "task": "Filter the index to documents that are published before 1987-06-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-06-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-06-01"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct range query structure. | [Field Validity] 'date' is the correct field. | [Operator Correctness] The comparison operator is flipped (gte\u2194lte), inverting the date window. This is a significant logic error that returns the opposite set of documents. | [Task Alignment] The field and structure are right but the result set is the complement of what was asked for."
  },
  {
    "task": "Get all documents discussing \"earn\" from 1987 onwards.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "earn"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "gte": "1987-01-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "earn"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] The bool query is valid but only has a 'must' clause \u2014 the 'filter' clause is entirely absent. | [Field Validity] The must clause field is correct. | [Operator Correctness] The must operator is correct; the filter operator doesn't exist to evaluate. | [Task Alignment] The query only satisfies the content-matching part of the task. The date/topic/country constraint is completely missing, returning too many documents."
  },
  {
    "task": "Filter by countryKeys to return only \"us\" documents.",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "us"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countries": "us"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct term query structure. | [Field Validity] 'countries' does not exist in the mapping at all \u2014 the correct field is 'countryKeys'. Fatal field name error. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Find documents where either the title or the content contains \"money-fx\".",
    "reference": {
      "query": {
        "multi_match": {
          "query": "money-fx",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "money-fx"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Valid match query but single-field \u2014 does not use multi_match. | [Field Validity] 'title' is a valid field. | [Operator Correctness] 'match' is wrong here \u2014 the task explicitly requires searching both title and content simultaneously, which requires 'multi_match'. | [Task Alignment] Returns only documents mentioning 'money-fx' in the title, missing all content-only mentions."
  },
  {
    "task": "Find all news articles whose body text mentions \"earn\".",
    "reference": {
      "query": {
        "match": {
          "content": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "earn"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct match structure. | [Field Validity] 'title' is a valid field but the task specifies 'content'. | [Operator Correctness] 'match' is correct for text fields. | [Task Alignment] Searches the wrong field \u2014 retrieves articles mentioning 'earn' in the title only, missing content-only mentions."
  },
  {
    "task": "Which Reuters articles have \"acq\" mentioned in the content?",
    "reference": {
      "query": {
        "match": {
          "content": "acq"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "body": "acq"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct match clause structure. | [Field Validity] The field 'body' does not exist in the mapping \u2014 correct field is 'content'. This causes zero results. | [Operator Correctness] 'match' operator is appropriate for text search. | [Task Alignment] Intent is correct but the non-existent field means no documents are retrieved."
  },
  {
    "task": "Retrieve articles that were published between 1987-01-01 and 1987-03-31.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-03-31"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-02-02",
            "lte": "1987-03-31"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct range query structure. | [Field Validity] 'date' field is correct. | [Operator Correctness] Operator direction is correct but boundary date is slightly off ('1987-02-02' vs '1987-01-01'). Minor precision issue. | [Task Alignment] Returns mostly correct documents but may miss or include a small number of edge documents due to the boundary difference."
  },
  {
    "task": "Get all documents whose topics field contains \"crude\".",
    "reference": {
      "query": {
        "term": {
          "topics": "crude"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": ""
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'topics' field is valid. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The value is empty string instead of 'crude' \u2014 returns zero relevant documents. Intent is partially right (correct field and operator) but the value is missing."
  },
  {
    "task": "Show me articles that belong to the \"earn\" topic.",
    "reference": {
      "query": {
        "term": {
          "topics": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": ""
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'topics' field is valid. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The value is empty string instead of 'earn' \u2014 returns zero relevant documents. Intent is partially right (correct field and operator) but the value is missing."
  },
  {
    "task": "Find all news articles whose body text mentions \"money-fx\".",
    "reference": {
      "query": {
        "match": {
          "content": "money-fx"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "content": "money-fx"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Query is structurally parseable. | [Field Validity] 'content' is a valid field. | [Operator Correctness] 'term' on a text field is a major operator error \u2014 'content' is analyzed/tokenized so term queries almost never match unless the value is a single token exactly as stored. | [Task Alignment] Will return near-zero results for 'money-fx' because analyzed text fields require 'match', not 'term'."
  },
  {
    "task": "Get all documents whose topics field contains \"money-fx\".",
    "reference": {
      "query": {
        "term": {
          "topics": "money-fx"
        }
      }
    },
    "submission": {
      "filter": {
        "term": {
          "topics": "trade"
        }
      },
      "sort": "date"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Top-level structure is invalid \u2014 not a valid Elasticsearch query. | [Field Validity] No valid mapping field is correctly targeted. | [Operator Correctness] No valid Elasticsearch operator is used correctly. | [Task Alignment] Would throw a parse error or return completely irrelevant documents."
  },
  {
    "task": "Get all documents discussing \"earn\" from 1987 onwards.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "earn"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "gte": "1987-01-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "should": [
            {
              "match": {
                "content": "earn"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Uses 'should' instead of 'must'+'filter' \u2014 fundamentally wrong boolean logic. 'should' makes clauses optional, not required. | [Field Validity] Field names in the should clause may be correct. | [Operator Correctness] The clause type 'should' is wrong for this task \u2014 it does not enforce any constraint. | [Task Alignment] Returns documents that optionally match the condition rather than requiring it, producing a semantically incorrect result set."
  },
  {
    "task": "Retrieve documents whose title contains \"money-fx\" and that predate June 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "lte": "1987-06-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "bool": {
        "must": [
          {
            "match": {
              "title": "value"
            }
          }
        ]
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely missing 'query' wrapper. | [Field Validity] No valid mapping fields are correctly referenced. | [Operator Correctness] No valid bool, must, or filter operators are correctly applied. | [Task Alignment] Would fail to parse or return completely irrelevant documents with no filtering."
  },
  {
    "task": "Retrieve articles that were published between 1987-01-01 and 1987-03-31.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-03-31"
          }
        }
      }
    },
    "submission": {
      "bool": {
        "must": [
          {
            "match": {
              "title": "value"
            }
          }
        ]
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely malformed DSL. | [Field Validity] No valid date field is correctly referenced. | [Operator Correctness] No range operator is applied. | [Task Alignment] Would fail to parse or return entirely irrelevant documents."
  },
  {
    "task": "Search for articles that discuss \"crude\" anywhere in their content.",
    "reference": {
      "query": {
        "match": {
          "content": "crude"
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "crude"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Unnecessarily wrapped in a bool/must but structurally valid; however no filter clause when none is needed is not wrong \u2014 this sub-case represents having the right match but inside the wrong clause type without any filter. | [Field Validity] 'content' field is correct. | [Operator Correctness] 'match' is correct. | [Task Alignment] This particular variant is effectively equivalent; for diversity this entry models the case where the bool wrapper adds no filter and the evaluator notes unnecessary complexity but correct results."
  },
  {
    "task": "Search for \"oil prices\" across both the title and content fields.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "oil prices",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "oil prices",
          "fields": [
            "headline",
            "body"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct multi_match structure. | [Field Validity] 'headline' and 'body' do not exist in the mapping \u2014 the correct fields are 'title' and 'content'. Both field names are wrong. | [Operator Correctness] 'multi_match' is the right operator. | [Task Alignment] Returns zero results because neither field exists in the index."
  },
  {
    "task": "Retrieve \"acq\" articles from oldest to newest.",
    "reference": {
      "query": {
        "term": {
          "topics": "acq"
        }
      },
      "sort": [
        {
          "date": {
            "order": "asc"
          }
        }
      ]
    },
    "submission": {
      "query": {
        "term": {
          "topics": "acq"
        }
      },
      "sort": [
        {
          "nonexistent_field": {
            "order": "asc"
          }
        }
      ]
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Query and sort clauses are both present and structurally correct. | [Field Validity] The query field is correct; the sort field 'nonexistent_field' does not exist in the mapping. | [Operator Correctness] Sort order 'asc' is correct. | [Task Alignment] The query part is perfect but sorting on a non-existent field will either error or fall back to default ordering, failing to satisfy the sort requirement."
  },
  {
    "task": "Which articles were published before 1987-06-01?",
    "reference": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-06-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-06-01"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct range query at the query level. | [Field Validity] 'date' is the valid date field in the mapping. | [Operator Correctness] Operators ['lte'] are exactly correct for 'published before 1987-06-01'. | [Task Alignment] Returns exactly the documents in the specified date window."
  },
  {
    "task": "Get all documents whose georeferences include an entry for country_code \"ca\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "ca"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "ca"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct nested query with 'path' and inner 'query' both present. | [Field Validity] 'georeferences' is a valid nested field; 'georeferences.country_code' is the correct dotted path. | [Operator Correctness] 'term' is correct for exact matching on the keyword subfield 'country_code'. | [Task Alignment] Returns exactly the documents with a nested georeference entry for country_code 'ca'."
  },
  {
    "task": "Filter by countryKeys to return only \"gb\" documents.",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "gb"
        }
      }
    },
    "submission": {
      "query": {
        "match_all": {}
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Syntactically valid but semantically empty \u2014 match_all returns every document. | [Field Validity] No field is targeted. | [Operator Correctness] match_all is not a filtering operator. | [Task Alignment] Returns all documents in the index with no filtering by country 'gb' \u2014 completely fails the task."
  },
  {
    "task": "Search for \"export\" across both the title and content fields.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "export",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "export"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Valid match query but single-field \u2014 does not use multi_match. | [Field Validity] 'title' is a valid field. | [Operator Correctness] 'match' is wrong here \u2014 the task explicitly requires searching both title and content simultaneously, which requires 'multi_match'. | [Task Alignment] Returns only documents mentioning 'export' in the title, missing all content-only mentions."
  },
  {
    "task": "Show me articles that belong to the \"grain\" topic.",
    "reference": {
      "query": {
        "term": {
          "topics": "grain"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": ""
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'topics' field is valid. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The value is empty string instead of 'grain' \u2014 returns zero relevant documents. Intent is partially right (correct field and operator) but the value is missing."
  },
  {
    "task": "Find articles from country \"gb\" published in 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match_all": {}
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "gb"
              }
            },
            {
              "range": {
                "date": {
                  "gte": "1987-01-01",
                  "lte": "1987-12-31"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match_all": {}
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "gb"
              }
            },
            {
              "range": {
                "date": {
                  "gte": "1987-01-01",
                  "lte": "1987-12-31"
                }
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct bool query with both 'must' and 'filter' clauses properly composed. | [Field Validity] All field names match the index mapping exactly. | [Operator Correctness] match/term/range operators are all appropriate for their respective field types. | [Task Alignment] Returns exactly the documents satisfying both the relevance and the constraint conditions."
  },
  {
    "task": "Retrieve articles that were published after 1987-09-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-09-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "published_at": {
            "gte": "1987-09-01"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct range query structure. | [Field Validity] 'published_at' does not exist in the mapping \u2014 the correct date field is 'date'. This is a fatal field name error. | [Operator Correctness] Operators are correct. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Retrieve every document where the content field contains the word \"earn\".",
    "reference": {
      "query": {
        "match": {
          "content": "earn"
        }
      }
    },
    "submission": {
      "filter": {
        "term": {
          "topics": "trade"
        }
      },
      "sort": "date"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Invalid top-level keys \u2014 not a valid Elasticsearch query DSL structure. | [Field Validity] Fields referenced either don't exist or are in wrong positions. | [Operator Correctness] No valid operator is correctly applied. | [Task Alignment] This query would either fail to parse or return completely unrelated results."
  },
  {
    "task": "Search for \"interest rate\" across both the title and content fields.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "interest rate",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "interest rate"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Valid match query but single-field \u2014 does not use multi_match. | [Field Validity] 'title' is a valid field. | [Operator Correctness] 'match' is wrong here \u2014 the task explicitly requires searching both title and content simultaneously, which requires 'multi_match'. | [Task Alignment] Returns only documents mentioning 'interest rate' in the title, missing all content-only mentions."
  },
  {
    "task": "Find all documents associated with country code \"jp\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "jp"
        }
      }
    },
    "submission": {
      "query": {
        "match_all": {}
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Syntactically valid but semantically empty \u2014 match_all returns every document. | [Field Validity] No field is targeted. | [Operator Correctness] match_all is not a filtering operator. | [Task Alignment] Returns all documents in the index with no filtering by country 'jp' \u2014 completely fails the task."
  },
  {
    "task": "Find all news articles whose body text mentions \"trade\".",
    "reference": {
      "query": {
        "match": {
          "content": "trade"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "trade"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct match structure. | [Field Validity] 'title' is a valid field but the task specifies 'content'. | [Operator Correctness] 'match' is correct for text fields. | [Task Alignment] Searches the wrong field \u2014 retrieves articles mentioning 'trade' in the title only, missing content-only mentions."
  },
  {
    "task": "Find all documents published in 1987.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-12-31"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-02-02",
            "lte": "1987-12-31"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct range query structure. | [Field Validity] 'date' field is correct. | [Operator Correctness] Operator direction is correct but boundary date is slightly off ('1987-02-02' vs '1987-01-01'). Minor precision issue. | [Task Alignment] Returns mostly correct documents but may miss or include a small number of edge documents due to the boundary difference."
  },
  {
    "task": "Filter by countryKeys to return only \"us\" documents.",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "us"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "places": "us"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'places' stores human-readable place name strings, not ISO country codes \u2014 the correct field is 'countryKeys'. Wrong field but exists in the mapping. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Will not find documents by country code 'us' since 'places' contains names not codes. Results will be empty or wrong."
  },
  {
    "task": "Find every article labelled under the topic category \"grain\".",
    "reference": {
      "query": {
        "term": {
          "topics": "grain"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": "grain"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct term query at the query level. | [Field Validity] 'topics' is a valid keyword field in the mapping. | [Operator Correctness] 'term' is the correct operator for exact-match on keyword fields. | [Task Alignment] Returns exactly documents tagged with topic 'grain'."
  },
  {
    "task": "Find all news articles whose body text mentions \"trade\".",
    "reference": {
      "query": {
        "match": {
          "content": "trade"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "content": "trade"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single match clause at the query level. | [Field Validity] 'content' is a valid text field in the mapping. | [Operator Correctness] 'match' is the correct operator for full-text search on a text field. | [Task Alignment] Returns exactly documents whose content mentions 'trade'."
  },
  {
    "task": "Retrieve every article linked to the country key \"fr\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "fr"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countryKeys": "fr"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single term query. | [Field Validity] 'countryKeys' is the valid keyword field for country codes. | [Operator Correctness] 'term' is the correct operator for exact match on a keyword field. | [Task Alignment] Returns exactly documents associated with country code 'fr'."
  },
  {
    "task": "Get all documents whose georeferences include an entry for country_code \"fr\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "fr"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georefs",
          "query": {
            "term": {
              "georeferences.country_code": "fr"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Nested wrapper structure is correct (path + query present). | [Field Validity] The nested path 'georefs' does not match the mapping \u2014 the correct path is 'georeferences'. The inner field name is correct. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The query cannot traverse the nested objects because the path is wrong \u2014 returns zero results despite correct inner query logic."
  },
  {
    "task": "Retrieve articles that were published after 1987-09-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-09-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-09-01"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct range query at the query level. | [Field Validity] 'date' is the valid date field in the mapping. | [Operator Correctness] Operators ['gte'] are exactly correct for 'published after 1987-09-01'. | [Task Alignment] Returns exactly the documents in the specified date window."
  },
  {
    "task": "Search for \"export\" across both the title and content fields.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "export",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "export",
          "fields": [
            "headline",
            "body"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct multi_match structure. | [Field Validity] 'headline' and 'body' do not exist in the mapping \u2014 the correct fields are 'title' and 'content'. Both field names are wrong. | [Operator Correctness] 'multi_match' is the right operator. | [Task Alignment] Returns zero results because neither field exists in the index."
  },
  {
    "task": "Multi-field search for \"oil prices\" covering title and content.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "oil prices",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "multi_match": {
          "query": "oil prices",
          "fields": [
            "title"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct multi_match structure. | [Field Validity] 'title' is a valid field. | [Operator Correctness] 'multi_match' is correct but underused \u2014 only one field is listed. | [Task Alignment] Searches only 'title', missing all documents where 'oil prices' appears only in 'content'. Half the required fields are covered."
  },
  {
    "task": "Find all news articles whose body text mentions \"acq\".",
    "reference": {
      "query": {
        "match": {
          "content": "acq"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "content": "acq"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Query is structurally parseable. | [Field Validity] 'content' is a valid field. | [Operator Correctness] 'term' on a text field is a major operator error \u2014 'content' is analyzed/tokenized so term queries almost never match unless the value is a single token exactly as stored. | [Task Alignment] Will return near-zero results for 'acq' because analyzed text fields require 'match', not 'term'."
  },
  {
    "task": "Retrieve articles that were published on or after 1987-03-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-03-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "terms": {
          "content": [
            "oil",
            "grain"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely malformed DSL. | [Field Validity] No valid date field is correctly referenced. | [Operator Correctness] No range operator is applied. | [Task Alignment] Would fail to parse or return entirely irrelevant documents."
  },
  {
    "task": "Get all documents whose georeferences include an entry for country_code \"jp\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "jp"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "match": {
              "georeferences.country_code": "jp"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct nested wrapper with 'path' and inner 'query'. | [Field Validity] Path and field name are both correct. | [Operator Correctness] 'match' is used on a keyword subfield where 'term' is required for exact matching. This is a minor operator precision issue. | [Task Alignment] Will mostly return correct results but 'match' may produce unexpected behavior on the keyword field 'georeferences.country_code'."
  },
  {
    "task": "Retrieve every document where the content field contains the word \"grain\".",
    "reference": {
      "query": {
        "match": {
          "content": "grain"
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "grain"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Unnecessarily wrapped in a bool/must but structurally valid; however no filter clause when none is needed is not wrong \u2014 this sub-case represents having the right match but inside the wrong clause type without any filter. | [Field Validity] 'content' field is correct. | [Operator Correctness] 'match' is correct. | [Task Alignment] This particular variant is effectively equivalent; for diversity this entry models the case where the bool wrapper adds no filter and the evaluator notes unnecessary complexity but correct results."
  },
  {
    "task": "Which documents have a nested georeference whose country_code equals \"jp\"?",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "jp"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georefs",
          "query": {
            "term": {
              "georeferences.country_code": "jp"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Nested wrapper structure is correct (path + query present). | [Field Validity] The nested path 'georefs' does not match the mapping \u2014 the correct path is 'georeferences'. The inner field name is correct. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The query cannot traverse the nested objects because the path is wrong \u2014 returns zero results despite correct inner query logic."
  },
  {
    "task": "Find articles from country \"gb\" published in 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match_all": {}
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "gb"
              }
            },
            {
              "range": {
                "date": {
                  "gte": "1987-01-01",
                  "lte": "1987-12-31"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "should": [
            {
              "match_all": {}
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Uses 'should' instead of 'must'+'filter' \u2014 fundamentally wrong boolean logic. 'should' makes clauses optional, not required. | [Field Validity] Field names in the should clause may be correct. | [Operator Correctness] The clause type 'should' is wrong for this task \u2014 it does not enforce any constraint. | [Task Alignment] Returns documents that optionally match the condition rather than requiring it, producing a semantically incorrect result set."
  },
  {
    "task": "Retrieve articles that were published on or after 1987-03-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-03-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "published_at": {
            "gte": "1987-03-01"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct range query structure. | [Field Validity] 'published_at' does not exist in the mapping \u2014 the correct date field is 'date'. This is a fatal field name error. | [Operator Correctness] Operators are correct. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Filter the index to documents that are published on or after 1987-03-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-03-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-03-01"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct range query at the query level. | [Field Validity] 'date' is the valid date field in the mapping. | [Operator Correctness] Operators ['gte'] are exactly correct for 'published on or after 1987-03-01'. | [Task Alignment] Returns exactly the documents in the specified date window."
  },
  {
    "task": "Find documents containing a georeference with country_code \"us\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "us"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "match": {
              "georeferences.country_code": "us"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct nested wrapper with 'path' and inner 'query'. | [Field Validity] Path and field name are both correct. | [Operator Correctness] 'match' is used on a keyword subfield where 'term' is required for exact matching. This is a minor operator precision issue. | [Task Alignment] Will mostly return correct results but 'match' may produce unexpected behavior on the keyword field 'georeferences.country_code'."
  },
  {
    "task": "Pull all documents whose content includes the keyword \"acq\".",
    "reference": {
      "query": {
        "match": {
          "content": "acq"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "content": "acq"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single match clause at the query level. | [Field Validity] 'content' is a valid text field in the mapping. | [Operator Correctness] 'match' is the correct operator for full-text search on a text field. | [Task Alignment] Returns exactly documents whose content mentions 'acq'."
  },
  {
    "task": "Retrieve every article linked to the country key \"br\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "br"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "places": "br"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'places' stores human-readable place name strings, not ISO country codes \u2014 the correct field is 'countryKeys'. Wrong field but exists in the mapping. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Will not find documents by country code 'br' since 'places' contains names not codes. Results will be empty or wrong."
  },
  {
    "task": "Retrieve all documents that are tagged with the topic \"earn\".",
    "reference": {
      "query": {
        "term": {
          "topics": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "terms": {
          "content": [
            "oil",
            "grain"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Top-level structure is invalid \u2014 not a valid Elasticsearch query. | [Field Validity] No valid mapping field is correctly targeted. | [Operator Correctness] No valid Elasticsearch operator is used correctly. | [Task Alignment] Would throw a parse error or return completely irrelevant documents."
  },
  {
    "task": "Get articles both discussing and categorised under \"acq\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "topics": "acq"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "bool": {
        "must": [
          {
            "match": {
              "title": "value"
            }
          }
        ]
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely missing 'query' wrapper. | [Field Validity] No valid mapping fields are correctly referenced. | [Operator Correctness] No valid bool, must, or filter operators are correctly applied. | [Task Alignment] Would fail to parse or return completely irrelevant documents with no filtering."
  },
  {
    "task": "Search inside the georeferences array for entries with country_code \"gb\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "gb"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "georeferences.country_code": "gb"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Missing the required 'nested' wrapper \u2014 a plain term query cannot traverse nested objects in Elasticsearch. | [Field Validity] The dotted field path 'georeferences.country_code' is correct. | [Operator Correctness] 'term' is the right operator but it's applied at the wrong query level. | [Task Alignment] Elasticsearch ignores plain term queries on nested fields \u2014 returns empty or wrong results."
  },
  {
    "task": "Retrieve articles that were published before 1987-06-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-06-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "published_at": {
            "lte": "1987-06-01"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct range query structure. | [Field Validity] 'published_at' does not exist in the mapping \u2014 the correct date field is 'date'. This is a fatal field name error. | [Operator Correctness] Operators are correct. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Get articles both discussing and categorised under \"acq\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "topics": "acq"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "should": [
            {
              "match": {
                "content": "acq"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Uses 'should' instead of 'must'+'filter' \u2014 fundamentally wrong boolean logic. 'should' makes clauses optional, not required. | [Field Validity] Field names in the should clause may be correct. | [Operator Correctness] The clause type 'should' is wrong for this task \u2014 it does not enforce any constraint. | [Task Alignment] Returns documents that optionally match the condition rather than requiring it, producing a semantically incorrect result set."
  },
  {
    "task": "Which articles were published in 1987?",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-12-31"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "published_at": {
            "gte": "1987-01-01",
            "lte": "1987-12-31"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct range query structure. | [Field Validity] 'published_at' does not exist in the mapping \u2014 the correct date field is 'date'. This is a fatal field name error. | [Operator Correctness] Operators are correct. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Find all news articles whose body text mentions \"acq\".",
    "reference": {
      "query": {
        "match": {
          "content": "acq"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "acq"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct match structure. | [Field Validity] 'title' is a valid field but the task specifies 'content'. | [Operator Correctness] 'match' is correct for text fields. | [Task Alignment] Searches the wrong field \u2014 retrieves articles mentioning 'acq' in the title only, missing content-only mentions."
  },
  {
    "task": "Search inside the georeferences array for entries with country_code \"fr\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "fr"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "fr"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct nested query with 'path' and inner 'query' both present. | [Field Validity] 'georeferences' is a valid nested field; 'georeferences.country_code' is the correct dotted path. | [Operator Correctness] 'term' is correct for exact matching on the keyword subfield 'country_code'. | [Task Alignment] Returns exactly the documents with a nested georeference entry for country_code 'fr'."
  },
  {
    "task": "Show me all records tagged with the country code \"fr\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "fr"
        }
      }
    },
    "submission": {
      "query": {
        "match_all": {}
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Syntactically valid but semantically empty \u2014 match_all returns every document. | [Field Validity] No field is targeted. | [Operator Correctness] match_all is not a filtering operator. | [Task Alignment] Returns all documents in the index with no filtering by country 'fr' \u2014 completely fails the task."
  },
  {
    "task": "Retrieve all documents that are tagged with the topic \"earn\".",
    "reference": {
      "query": {
        "term": {
          "topics": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "topics": "earn"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Query structure is valid. | [Field Validity] 'topics' field is correct. | [Operator Correctness] 'match' is imprecise on a keyword field \u2014 it performs analysis that may split or normalize the value, causing unexpected matches or misses. 'term' is required for exact keyword matching. | [Task Alignment] May return broadly correct results but with precision issues for multi-word topic values like 'earn'."
  },
  {
    "task": "Get documents where countryKeys includes \"ca\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "ca"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "countryKeys": "ca"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Valid query structure. | [Field Validity] 'countryKeys' is the correct field. | [Operator Correctness] 'match' on a keyword field is imprecise \u2014 country codes should be matched exactly with 'term', not analyzed with 'match'. | [Task Alignment] May mostly work for single-token country codes like 'ca' but is semantically wrong and fragile for multi-part codes."
  },
  {
    "task": "Filter the index to documents tagged \"earn\" in their topics list.",
    "reference": {
      "query": {
        "term": {
          "topics": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "tags": "earn"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Structurally valid term query. | [Field Validity] 'tags' does not exist in the index mapping \u2014 the correct field is 'topics'. This is a critical field name error. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Returns zero results because the field doesn't exist. The intent is clear but the field name error is fatal."
  },
  {
    "task": "Find documents containing a georeference with country_code \"fr\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "fr"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "match": {
              "georeferences.country_code": "fr"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct nested wrapper with 'path' and inner 'query'. | [Field Validity] Path and field name are both correct. | [Operator Correctness] 'match' is used on a keyword subfield where 'term' is required for exact matching. This is a minor operator precision issue. | [Task Alignment] Will mostly return correct results but 'match' may produce unexpected behavior on the keyword field 'georeferences.country_code'."
  },
  {
    "task": "Get documents where countryKeys includes \"jp\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "jp"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "places": "jp"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'places' stores human-readable place name strings, not ISO country codes \u2014 the correct field is 'countryKeys'. Wrong field but exists in the mapping. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Will not find documents by country code 'jp' since 'places' contains names not codes. Results will be empty or wrong."
  },
  {
    "task": "Get every document published in 1987.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-12-31"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-12-31"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct range query structure. | [Field Validity] 'date' is the correct field. | [Operator Correctness] The comparison operator is flipped (gte\u2194lte), inverting the date window. This is a significant logic error that returns the opposite set of documents. | [Task Alignment] The field and structure are right but the result set is the complement of what was asked for."
  },
  {
    "task": "Retrieve \"acq\" articles from oldest to newest.",
    "reference": {
      "query": {
        "term": {
          "topics": "acq"
        }
      },
      "sort": [
        {
          "date": {
            "order": "asc"
          }
        }
      ]
    },
    "submission": {
      "query": {
        "term": {
          "topics": "acq"
        }
      },
      "sort": [
        {
          "date": {
            "order": "asc"
          }
        }
      ]
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct query clause plus a valid top-level 'sort' array. | [Field Validity] Both the query field and the sort field ('date') exist in the mapping. | [Operator Correctness] Sort order 'asc' is correct for the task. | [Task Alignment] Returns the right documents in the correct order."
  },
  {
    "task": "Get every document published before 1987-06-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-06-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "lte": "1987-06-02"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct range query structure. | [Field Validity] 'date' field is correct. | [Operator Correctness] Operator direction is correct but boundary date is slightly off ('1987-06-02' vs '1987-06-01'). Minor precision issue. | [Task Alignment] Returns mostly correct documents but may miss or include a small number of edge documents due to the boundary difference."
  },
  {
    "task": "Retrieve documents whose title contains \"money-fx\" and that predate June 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "lte": "1987-06-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "should": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Uses 'should' instead of 'must'+'filter' \u2014 fundamentally wrong boolean logic. 'should' makes clauses optional, not required. | [Field Validity] Field names in the should clause may be correct. | [Operator Correctness] The clause type 'should' is wrong for this task \u2014 it does not enforce any constraint. | [Task Alignment] Returns documents that optionally match the condition rather than requiring it, producing a semantically incorrect result set."
  },
  {
    "task": "Find all news articles whose body text mentions \"earn\".",
    "reference": {
      "query": {
        "match": {
          "content": "earn"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "content": "earn"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single match clause at the query level. | [Field Validity] 'content' is a valid text field in the mapping. | [Operator Correctness] 'match' is the correct operator for full-text search on a text field. | [Task Alignment] Returns exactly documents whose content mentions 'earn'."
  },
  {
    "task": "Retrieve documents whose title contains \"money-fx\" and that predate June 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "lte": "1987-06-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "title": "money-fx"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "published_at": {
                  "lte": "1987-06-01"
                }
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct bool structure with must and filter clauses. | [Field Validity] The must clause field is correct; the filter clause references a non-existent field \u2014 a single field name fix is all that's needed. | [Operator Correctness] Operators (match/term/range) are all correct. | [Task Alignment] The query logic is right but the invalid filter field means the constraint is not applied, making the result set over-broad."
  },
  {
    "task": "Which documents have a nested georeference whose country_code equals \"fr\"?",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "fr"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "georeferences.country_code": "fr"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Missing the required 'nested' wrapper \u2014 a plain term query cannot traverse nested objects in Elasticsearch. | [Field Validity] The dotted field path 'georeferences.country_code' is correct. | [Operator Correctness] 'term' is the right operator but it's applied at the wrong query level. | [Task Alignment] Elasticsearch ignores plain term queries on nested fields \u2014 returns empty or wrong results."
  },
  {
    "task": "Run a search for \"earn\" in title and content simultaneously.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "earn",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "earn"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Valid match query but single-field \u2014 does not use multi_match. | [Field Validity] 'title' is a valid field. | [Operator Correctness] 'match' is wrong here \u2014 the task explicitly requires searching both title and content simultaneously, which requires 'multi_match'. | [Task Alignment] Returns only documents mentioning 'earn' in the title, missing all content-only mentions."
  },
  {
    "task": "Retrieve articles associated with country code \"us\" that discuss \"grain\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "grain"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "us"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "grain"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "countries": "us"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct bool structure with must and filter clauses. | [Field Validity] The must clause field is correct; the filter clause references a non-existent field \u2014 a single field name fix is all that's needed. | [Operator Correctness] Operators (match/term/range) are all correct. | [Task Alignment] The query logic is right but the invalid filter field means the constraint is not applied, making the result set over-broad."
  }
]