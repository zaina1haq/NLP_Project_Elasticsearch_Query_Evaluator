[
  {
    "task": "Find all documents associated with country code \"ca\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "ca"
        }
      }
    },
    "submission": {
      "query": {
        "match_all": {}
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Syntactically valid but semantically empty \u2014 match_all returns every document. | [Field Validity] No field is targeted. | [Operator Correctness] match_all is not a filtering operator. | [Task Alignment] Returns all documents in the index with no filtering by country 'ca' \u2014 completely fails the task."
  },
  {
    "task": "Filter by countryKeys to return only \"fr\" documents.",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "fr"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "countryKeys": "fr"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Valid query structure. | [Field Validity] 'countryKeys' is the correct field. | [Operator Correctness] 'match' on a keyword field is imprecise \u2014 country codes should be matched exactly with 'term', not analyzed with 'match'. | [Task Alignment] May mostly work for single-token country codes like 'fr' but is semantically wrong and fragile for multi-part codes."
  },
  {
    "task": "Retrieve every document where the content field contains the word \"money-fx\".",
    "reference": {
      "query": {
        "match": {
          "content": "money-fx"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "money-fx"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct match structure. | [Field Validity] 'title' is a valid field but the task specifies 'content'. | [Operator Correctness] 'match' is correct for text fields. | [Task Alignment] Searches the wrong field \u2014 retrieves articles mentioning 'money-fx' in the title only, missing content-only mentions."
  },
  {
    "task": "Retrieve \"acq\" articles from oldest to newest.",
    "reference": {
      "query": {
        "term": {
          "topics": "acq"
        }
      },
      "sort": [
        {
          "date": {
            "order": "asc"
          }
        }
      ]
    },
    "submission": {
      "query": {
        "term": {
          "topics": "acq"
        }
      },
      "sort": [
        {
          "date": {
            "order": "desc"
          }
        }
      ]
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Query and sort clauses are present. | [Field Validity] All field names are correct. | [Operator Correctness] Sort order is 'desc' but the task requires 'asc' \u2014 the direction is exactly backwards. | [Task Alignment] Returns the right documents but in the opposite order, completely inverting the intended ranking."
  },
  {
    "task": "Retrieve articles associated with country code \"us\" that discuss \"grain\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "grain"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "us"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "search": {
        "match": {
          "content": "grain"
        }
      },
      "order": "desc"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely missing 'query' wrapper. | [Field Validity] No valid mapping fields are correctly referenced. | [Operator Correctness] No valid bool, must, or filter operators are correctly applied. | [Task Alignment] Would fail to parse or return completely irrelevant documents with no filtering."
  },
  {
    "task": "Find articles from country \"gb\" published in 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match_all": {}
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "gb"
              }
            },
            {
              "range": {
                "date": {
                  "gte": "1987-01-01",
                  "lte": "1987-12-31"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match_all": {}
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] The bool query is valid but only has a 'must' clause \u2014 the 'filter' clause is entirely absent. | [Field Validity] The must clause field is correct. | [Operator Correctness] The must operator is correct; the filter operator doesn't exist to evaluate. | [Task Alignment] The query only satisfies the content-matching part of the task. The date/topic/country constraint is completely missing, returning too many documents."
  },
  {
    "task": "Get all documents discussing \"earn\" from 1987 onwards.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "earn"
              }
            }
          ],
          "filter": [
            {
              "range": {
                "date": {
                  "gte": "1987-01-01"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "search": {
        "match": {
          "content": "grain"
        }
      },
      "order": "desc"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely missing 'query' wrapper. | [Field Validity] No valid mapping fields are correctly referenced. | [Operator Correctness] No valid bool, must, or filter operators are correctly applied. | [Task Alignment] Would fail to parse or return completely irrelevant documents with no filtering."
  },
  {
    "task": "Find articles from country \"gb\" published in 1987.",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match_all": {}
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "gb"
              }
            },
            {
              "range": {
                "date": {
                  "gte": "1987-01-01",
                  "lte": "1987-12-31"
                }
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "filter": {
            "match": {
              "content": "oil"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Not a valid Elasticsearch query \u2014 wrong top-level keys or completely missing 'query' wrapper. | [Field Validity] No valid mapping fields are correctly referenced. | [Operator Correctness] No valid bool, must, or filter operators are correctly applied. | [Task Alignment] Would fail to parse or return completely irrelevant documents with no filtering."
  },
  {
    "task": "Find all documents associated with country code \"fr\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "fr"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "places": "fr"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'places' stores human-readable place name strings, not ISO country codes \u2014 the correct field is 'countryKeys'. Wrong field but exists in the mapping. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Will not find documents by country code 'fr' since 'places' contains names not codes. Results will be empty or wrong."
  },
  {
    "task": "Multi-field search for \"earn\" covering title and content.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "earn",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "terms": {
          "content": [
            "oil",
            "grain"
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Invalid Elasticsearch DSL \u2014 wrong top-level keys or missing 'query' wrapper. | [Field Validity] No valid mapping fields are correctly referenced. | [Operator Correctness] No valid operator is applied. | [Task Alignment] Would fail to parse or return completely unrelated documents."
  },
  {
    "task": "Retrieve every document where the content field contains the word \"crude\".",
    "reference": {
      "query": {
        "match": {
          "content": "crude"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "content": "crude"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single match clause at the query level. | [Field Validity] 'content' is a valid text field in the mapping. | [Operator Correctness] 'match' is the correct operator for full-text search on a text field. | [Task Alignment] Returns exactly documents whose content mentions 'crude'."
  },
  {
    "task": "Retrieve all documents that are tagged with the topic \"money-fx\".",
    "reference": {
      "query": {
        "term": {
          "topics": "money-fx"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "topics": ""
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct term query structure. | [Field Validity] 'topics' field is valid. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The value is empty string instead of 'money-fx' \u2014 returns zero relevant documents. Intent is partially right (correct field and operator) but the value is missing."
  },
  {
    "task": "Retrieve articles associated with country code \"us\" that discuss \"grain\".",
    "reference": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "grain"
              }
            }
          ],
          "filter": [
            {
              "term": {
                "countryKeys": "us"
              }
            }
          ]
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "should": [
            {
              "match": {
                "content": "grain"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Uses 'should' instead of 'must'+'filter' \u2014 fundamentally wrong boolean logic. 'should' makes clauses optional, not required. | [Field Validity] Field names in the should clause may be correct. | [Operator Correctness] The clause type 'should' is wrong for this task \u2014 it does not enforce any constraint. | [Task Alignment] Returns documents that optionally match the condition rather than requiring it, producing a semantically incorrect result set."
  },
  {
    "task": "Retrieve articles mentioning \"oil prices\" in their title or body text.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "oil prices",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "title": "oil prices"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Valid match query but single-field \u2014 does not use multi_match. | [Field Validity] 'title' is a valid field. | [Operator Correctness] 'match' is wrong here \u2014 the task explicitly requires searching both title and content simultaneously, which requires 'multi_match'. | [Task Alignment] Returns only documents mentioning 'oil prices' in the title, missing all content-only mentions."
  },
  {
    "task": "Retrieve articles that were published on or after 1987-03-01.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-03-01"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-03-02"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Correct range query structure. | [Field Validity] 'date' field is correct. | [Operator Correctness] Operator direction is correct but boundary date is slightly off ('1987-03-02' vs '1987-03-01'). Minor precision issue. | [Task Alignment] Returns mostly correct documents but may miss or include a small number of edge documents due to the boundary difference."
  },
  {
    "task": "Find documents containing a georeference with country_code \"us\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "us"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "us"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct nested query with 'path' and inner 'query' both present. | [Field Validity] 'georeferences' is a valid nested field; 'georeferences.country_code' is the correct dotted path. | [Operator Correctness] 'term' is correct for exact matching on the keyword subfield 'country_code'. | [Task Alignment] Returns exactly the documents with a nested georeference entry for country_code 'us'."
  },
  {
    "task": "Get documents where countryKeys includes \"us\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "us"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "countryKeys": "us"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 3,
    "rationale": "[Score 3] [Query Structure] Valid query structure. | [Field Validity] 'countryKeys' is the correct field. | [Operator Correctness] 'match' on a keyword field is imprecise \u2014 country codes should be matched exactly with 'term', not analyzed with 'match'. | [Task Alignment] May mostly work for single-token country codes like 'us' but is semantically wrong and fragile for multi-part codes."
  },
  {
    "task": "Find all documents associated with country code \"us\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "us"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countryKeys": "us"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single term query. | [Field Validity] 'countryKeys' is the valid keyword field for country codes. | [Operator Correctness] 'term' is the correct operator for exact match on a keyword field. | [Task Alignment] Returns exactly documents associated with country code 'us'."
  },
  {
    "task": "Retrieve articles mentioning \"money-fx\" in their title or body text.",
    "reference": {
      "query": {
        "multi_match": {
          "query": "money-fx",
          "fields": [
            "title",
            "content"
          ]
        }
      }
    },
    "submission": {
      "search": {
        "match": {
          "content": "grain"
        }
      },
      "order": "desc"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Invalid Elasticsearch DSL \u2014 wrong top-level keys or missing 'query' wrapper. | [Field Validity] No valid mapping fields are correctly referenced. | [Operator Correctness] No valid operator is applied. | [Task Alignment] Would fail to parse or return completely unrelated documents."
  },
  {
    "task": "Find all news articles whose body text mentions \"trade\".",
    "reference": {
      "query": {
        "match": {
          "content": "trade"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "body": "trade"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Correct match clause structure. | [Field Validity] The field 'body' does not exist in the mapping \u2014 correct field is 'content'. This causes zero results. | [Operator Correctness] 'match' operator is appropriate for text search. | [Task Alignment] Intent is correct but the non-existent field means no documents are retrieved."
  },
  {
    "task": "Find documents containing a georeference with country_code \"ca\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "ca"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georefs",
          "query": {
            "term": {
              "georeferences.country_code": "ca"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Nested wrapper structure is correct (path + query present). | [Field Validity] The nested path 'georefs' does not match the mapping \u2014 the correct path is 'georeferences'. The inner field name is correct. | [Operator Correctness] 'term' is the right operator. | [Task Alignment] The query cannot traverse the nested objects because the path is wrong \u2014 returns zero results despite correct inner query logic."
  },
  {
    "task": "Get all documents whose topics field contains \"grain\".",
    "reference": {
      "query": {
        "term": {
          "topics": "grain"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "tags": "grain"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Structurally valid term query. | [Field Validity] 'tags' does not exist in the index mapping \u2014 the correct field is 'topics'. This is a critical field name error. | [Operator Correctness] 'term' is the right operator type. | [Task Alignment] Returns zero results because the field doesn't exist. The intent is clear but the field name error is fatal."
  },
  {
    "task": "Retrieve articles that have a georeference entry where country_code is \"us\".",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "us"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "georeferences.country_code": "us"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Missing the required 'nested' wrapper \u2014 a plain term query cannot traverse nested objects in Elasticsearch. | [Field Validity] The dotted field path 'georeferences.country_code' is correct. | [Operator Correctness] 'term' is the right operator but it's applied at the wrong query level. | [Task Alignment] Elasticsearch ignores plain term queries on nested fields \u2014 returns empty or wrong results."
  },
  {
    "task": "Find all documents associated with country code \"br\".",
    "reference": {
      "query": {
        "term": {
          "countryKeys": "br"
        }
      }
    },
    "submission": {
      "query": {
        "term": {
          "countryKeys": "br"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single term query. | [Field Validity] 'countryKeys' is the valid keyword field for country codes. | [Operator Correctness] 'term' is the correct operator for exact match on a keyword field. | [Task Alignment] Returns exactly documents associated with country code 'br'."
  },
  {
    "task": "Retrieve articles that were published in 1987.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-12-31"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-12-31"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct range query at the query level. | [Field Validity] 'date' is the valid date field in the mapping. | [Operator Correctness] Operators ['gte', 'lte'] are exactly correct for 'published in 1987'. | [Task Alignment] Returns exactly the documents in the specified date window."
  },
  {
    "task": "Get all documents whose topics field contains \"crude\".",
    "reference": {
      "query": {
        "term": {
          "topics": "crude"
        }
      }
    },
    "submission": {
      "filter": {
        "term": {
          "topics": "trade"
        }
      },
      "sort": "date"
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Top-level structure is invalid \u2014 not a valid Elasticsearch query. | [Field Validity] No valid mapping field is correctly targeted. | [Operator Correctness] No valid Elasticsearch operator is used correctly. | [Task Alignment] Would throw a parse error or return completely irrelevant documents."
  },
  {
    "task": "Which documents have a nested georeference whose country_code equals \"jp\"?",
    "reference": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "jp"
            }
          }
        }
      }
    },
    "submission": {
      "query": {
        "nested": {
          "path": "georeferences",
          "query": {
            "term": {
              "georeferences.country_code": "jp"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct nested query with 'path' and inner 'query' both present. | [Field Validity] 'georeferences' is a valid nested field; 'georeferences.country_code' is the correct dotted path. | [Operator Correctness] 'term' is correct for exact matching on the keyword subfield 'country_code'. | [Task Alignment] Returns exactly the documents with a nested georeference entry for country_code 'jp'."
  },
  {
    "task": "Pull all documents whose content includes the keyword \"grain\".",
    "reference": {
      "query": {
        "match": {
          "content": "grain"
        }
      }
    },
    "submission": {
      "query": {
        "match": {
          "content": "grain"
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 4,
    "rationale": "[Score 4] [Query Structure] Correct single match clause at the query level. | [Field Validity] 'content' is a valid text field in the mapping. | [Operator Correctness] 'match' is the correct operator for full-text search on a text field. | [Task Alignment] Returns exactly documents whose content mentions 'grain'."
  },
  {
    "task": "Retrieve articles that were published between 1987-01-01 and 1987-03-31.",
    "reference": {
      "query": {
        "range": {
          "date": {
            "gte": "1987-01-01",
            "lte": "1987-03-31"
          }
        }
      }
    },
    "submission": {
      "query": {
        "range": {
          "published_at": {
            "gte": "1987-01-01",
            "lte": "1987-03-31"
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 1,
    "rationale": "[Score 1] [Query Structure] Correct range query structure. | [Field Validity] 'published_at' does not exist in the mapping \u2014 the correct date field is 'date'. This is a fatal field name error. | [Operator Correctness] Operators are correct. | [Task Alignment] Returns zero results because the field doesn't exist in the index."
  },
  {
    "task": "Pull all documents whose content includes the keyword \"acq\".",
    "reference": {
      "query": {
        "match": {
          "content": "acq"
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "must": [
            {
              "match": {
                "content": "acq"
              }
            }
          ]
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 2,
    "rationale": "[Score 2] [Query Structure] Unnecessarily wrapped in a bool/must but structurally valid; however no filter clause when none is needed is not wrong \u2014 this sub-case represents having the right match but inside the wrong clause type without any filter. | [Field Validity] 'content' field is correct. | [Operator Correctness] 'match' is correct. | [Task Alignment] This particular variant is effectively equivalent; for diversity this entry models the case where the bool wrapper adds no filter and the evaluator notes unnecessary complexity but correct results."
  },
  {
    "task": "Pull all documents whose content includes the keyword \"money-fx\".",
    "reference": {
      "query": {
        "match": {
          "content": "money-fx"
        }
      }
    },
    "submission": {
      "query": {
        "bool": {
          "filter": {
            "match": {
              "content": "oil"
            }
          }
        }
      }
    },
    "rubric": {
      "0": "Score 0 \u2014 Completely invalid: [Query Structure] The JSON is unparseable or uses entirely wrong top-level keys (e.g. 'filter' or 'order' at the root instead of 'query'/'sort'). [Field Validity] Fields used do not exist in the index mapping at all. [Operator Correctness] No recognizable Elasticsearch query operator is used correctly. [Task Alignment] The query is completely unrelated to the stated task \u2014 it would either throw a parsing error or return results that have nothing to do with what was asked.",
      "1": "Score 1 \u2014 Major errors: [Query Structure] A recognizable query type is attempted (e.g. bool, match, term) but the structure is fundamentally broken \u2014 e.g. a nested field queried without a nested wrapper, or a plain term used where a bool is required. [Field Validity] At least one critical field name is wrong or non-existent (e.g. 'headline' instead of 'title', 'tags' instead of 'topics'). [Operator Correctness] Wrong clause type used that changes semantics significantly (e.g. match_all instead of a specific filter). [Task Alignment] The query captures the right intent but the errors are severe enough that it would return empty results or completely wrong documents.",
      "2": "Score 2 \u2014 Partially correct: [Query Structure] The outer query structure is correct (e.g. bool with must/filter) but at least one required clause is entirely missing (e.g. no filter, no sort, no date range, missing one branch of a multi-field search). [Field Validity] All field names that ARE present are correct. [Operator Correctness] Operators used are correct where applied; errors are of omission, not commission. [Task Alignment] The query partially satisfies the task \u2014 it gets some results right but the missing component means the result set is over-broad or incomplete.",
      "3": "Score 3 \u2014 Mostly correct with minor issues: [Query Structure] All required clauses and nesting levels are present. [Field Validity] At most one field name is slightly wrong or suboptimal (e.g. using a non-existent field name in sort, or using 'match' on a keyword field where 'term' is preferred). [Operator Correctness] Operators are logically correct but one is imprecise \u2014 e.g. match vs term on a keyword field, or a sort on a non-indexed field. [Task Alignment] The query would return broadly correct results but with minor precision or correctness issues that a small one-line fix would resolve.",
      "4": "Score 4 \u2014 Fully correct: [Query Structure] The query uses exactly the right clause type(s), correctly nested and composed (e.g. proper bool > must + filter, nested wrapper with correct path). [Field Validity] Every field name is exactly correct and matches the index mapping. [Operator Correctness] All operators (match/term/range/sort order/nested path) are exactly right for the data type of each field. [Task Alignment] The query returns precisely the documents the task describes \u2014 no over-retrieval, no under-retrieval, and the sort/pagination is correct if specified."
    },
    "score": 0,
    "rationale": "[Score 0] [Query Structure] Invalid top-level keys \u2014 not a valid Elasticsearch query DSL structure. | [Field Validity] Fields referenced either don't exist or are in wrong positions. | [Operator Correctness] No valid operator is correctly applied. | [Task Alignment] This query would either fail to parse or return completely unrelated results."
  }
]